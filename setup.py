from setuptools import setup

setup(name='xystage',
      version='0.1',
      description='Code to control the xy stage antenna pattern mapper and other Arduino hardware.',
      url='https://github.com/ColumbiaCMB/xystage',
      author='Columbia experimental CMB group',
      license='GPLv3',
      packages=['xystage'],
      zip_safe=False)
